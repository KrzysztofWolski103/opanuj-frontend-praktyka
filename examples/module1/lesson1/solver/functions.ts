export function f1(a, b) {
  return a + b;
}
export function f2(a, b) {
  return a - b;
}
export function f3(a, b) {
  return a * b;
}

export function f4(a, b) {
  return a / b;
}




// Przeprowadź refaktoryzację kodu w przykładzie examples/module1/lesson1/solver

// 1. Jeśli nie pracowałeś do tej pory z Reactem, skup się na tych fragmentach projektu, które nie są ściśle powiązane z samym frameworkiem (np. style i logika biznesowa).

// 2. Jakie trzy usprawnienia wprowadzisz, aby ułatwić dalszy rozwój projektu?

// 3. Czy uda ci się znaleźć więcej okazji do refaktoryzacji tej aplikacji?

// 4. Jak wprowadzić obsługę błędów na wypadek niepoprawnych danych wejściowych?